{% set route = app.request.attributes.get('_route') %}
<nav class="bg-white text-black shadow-lg sticky top-0 z-50">
    <div class="container mx-auto px-6 py-2 flex justify-between items-center h-16">
        <!-- Logo Section -->
        {# <a href="{{ path('accueil') }}" class="text-3xl font-bold text-white">
            Joel<span class="text-joel-red">Location</span>
        </a> #}
        <!-- Logo Section -->
        <div class="flex items-center space-x-4">
            <a href="{{ path('accueil') }}" class="flex items-center">
                <img src="{{ asset('images/Joel-Location-new.png') }}" alt="JOEL LOCATION" style="height: 110px;">
                <div class="ml-3 hidden lg:block">
                    <div class="flex flex-col">
                        <span class="text-gray-700 text-lg italic" style="font-family:'Monotype Corsiva'; font-size:18px;">
                            Sa Ka Woulé
                        </span>
                    </div>
                </div>
            </a>
        </div>

        <!-- Desktop Navigation -->
        <div class="hidden md:flex space-x-8">
            <a href="{{ path('accueil') }}"
               class="hover:text-joel-red transition-colors duration-300 {{ route starts with 'accueil' ? 'text-joel-red' : '' }}">
                Accueil
            </a>
            <a href="{{ path('quisommenous') }}"
               class="hover:text-joel-red transition-colors duration-300 {{ route starts with 'quisommenous' ? 'text-joel-red' : '' }}">
                À propos
            </a>
            <a href="{{ path('nosvehicules') }}"
               class="hover:text-joel-red transition-colors duration-300 {{ route starts with 'nosvehicules' ? 'text-joel-red' : '' }}">
                Nos véhicules
            </a>
            <a href="{{ path('formulaire-contact') }}"
               class="hover:text-joel-red transition-colors duration-300 {{ route starts with 'formulaire-contact' ? 'text-joel-red' : '' }}">
                Contact
            </a>
            <a href="{{ path('foireauxquestion') }}"
               class="hover:text-joel-red transition-colors duration-300 {{ route starts with 'foireauxquestion' ? 'text-joel-red' : '' }}">
                FAQ
            </a>
        </div>

        <!-- Droite : Réseaux + CTA -->
        <div class="hidden md:flex items-center space-x-5">
            <!-- Liens Réseaux Sociaux -->
            <div class="flex space-x-4">
                <a href="https://www.facebook.com/joellocation"
                   target="_blank"
                   class="hover:text-joel-red transition-colors duration-300"
                   aria-label="Facebook">
                    <i class="fab fa-facebook-f fa-lg"></i>
                </a>
                <a href="https://www.instagram.com/joel.location/"
                   target="_blank"
                   class="hover:text-joel-red transition-colors duration-300"
                   aria-label="Instagram">
                    <i class="fab fa-instagram fa-lg"></i>
                </a>
                <a href="https://wa.me/+330767321447"
                   target="_blank"
                   class="hover:text-joel-red transition-colors duration-300"
                   aria-label="WhatsApp">
                    <i class="fab fa-whatsapp fa-lg"></i>
                </a>
                <a href="https://youtube.com/channel/UCMZZRNgOmDBIghZwg4tTQJQ"
                   target="_blank"
                   class="hover:text-joel-red transition-colors duration-300"
                   aria-label="YouTube">
                    <i class="fab fa-youtube fa-lg"></i>
                </a>
            </div>

            <!-- Séparateur visuel vertical -->
            <div class="h-6 w-px bg-gray-600"></div>

            <!-- Bouton Call-to-Action -->
            <div class="flex items-center space-x-3">
                <a href="{{ path('reserver_maintenant') }}"
                   class="bg-joel-red text-white font-bold px-5 py-2 rounded-md hover:bg-joel-red-dark transition-all duration-300 uppercase text-sm shadow-md hover:shadow-lg transform hover:-translate-y-0.5 focus:outline-none">
                    Réserver
                </a>
                {% if is_granted("ROLE_CLIENT") or is_granted("ROLE_PERSONNEL") %}
                    <div class="relative group">
                        <button class="bg-gray-300 text-gray-700 font-bold px-5 py-2 rounded-md hover:bg-gray-400 transition-all duration-300 uppercase text-sm">
                            Mon Compte
                        </button>
                        <div class="absolute right-0 mt-2 w-48 bg-white rounded-md shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
                            <div class="py-2">
                                <a href="{% if is_granted('ROLE_CLIENT') %}{{ path('espaceClient_index') }}{% else %}{{ path('admin_index') }}{% endif %}"
                                   class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors duration-200">
                                    Mon compte
                                </a>
                                <a href="{{ path('app_logout') }}"
                                   class="block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition-colors duration-200">
                                    Déconnexion
                                </a>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <a href="{{ path('espaceClient_index') }}"
                       class="bg-gray-300 text-gray-700 font-bold px-5 py-2 rounded-md hover:bg-gray-400 transition-all duration-300 uppercase text-sm">
                        Mon Compte
                    </a>
                {% endif %}
            </div>
        </div>

        <!-- Bouton Menu Mobile -->
        <div class="md:hidden flex items-center">
            <button id="mobile-menu-button" class="text-gray-700 focus:outline-none">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Menu Mobile -->
    <div id="mobile-menu" class="md:hidden hidden bg-white shadow-xl">
        <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
            <!-- Liens de navigation mobiles -->
            <a href="{{ path('accueil') }}"
               class="block px-6 py-3 text-base text-gray-700 hover:bg-gray-100 transition-colors duration-300 {{ route starts with 'accueil' ? 'text-joel-red bg-gray-100' : '' }}">
                Accueil
            </a>
            <a href="{{ path('quisommenous') }}"
               class="block px-6 py-3 text-base text-gray-700 hover:bg-gray-100 transition-colors duration-300 {{ route starts with 'quisommenous' ? 'text-joel-red bg-gray-100' : '' }}">
                À propos
            </a>
            <a href="{{ path('nosvehicules') }}"
               class="block px-6 py-3 text-base text-gray-700 hover:bg-gray-100 transition-colors duration-300 {{ route starts with 'nosvehicules' ? 'text-joel-red bg-gray-100' : '' }}">
                Nos véhicules
            </a>
            <a href="{{ path('formulaire-contact') }}"
               class="block px-6 py-3 text-base text-gray-700 hover:bg-gray-100 transition-colors duration-300 {{ route starts with 'formulaire-contact' ? 'text-joel-red bg-gray-100' : '' }}">
                Contact
            </a>
            <a href="{{ path('foireauxquestion') }}"
               class="block px-6 py-3 text-base text-gray-700 hover:bg-gray-100 transition-colors duration-300 {{ route starts with 'foireauxquestion' ? 'text-joel-red bg-gray-100' : '' }}">
                FAQ
            </a>

            <!-- CTA dans le menu mobile -->
            <div class="px-6 py-4 space-y-3 border-t border-gray-200">
                <a href="{{ path('reserver_maintenant') }}"
                   class="block w-full text-center bg-joel-red text-white font-bold px-5 py-3 rounded-md hover:bg-joel-red-dark transition-colors duration-300 uppercase text-sm">
                    Réserver
                </a>
                {% if is_granted("ROLE_CLIENT") or is_granted("ROLE_PERSONNEL") %}
                    <a href="{% if is_granted('ROLE_CLIENT') %}{{ path('espaceClient_index') }}{% else %}{{ path('admin_index') }}{% endif %}"
                       class="block w-full text-center bg-gray-200 text-gray-700 font-bold px-5 py-3 rounded-md hover:bg-gray-300 transition-colors duration-300 uppercase text-sm">
                        Mon Compte
                    </a>
                    <a href="{{ path('app_logout') }}"
                       class="block w-full text-center bg-gray-600 text-white font-bold px-5 py-3 rounded-md hover:bg-gray-700 transition-colors duration-300 uppercase text-sm">
                        Déconnexion
                    </a>
                {% else %}
                    <a href="{{ path('espaceClient_index') }}"
                       class="block w-full text-center bg-gray-200 text-gray-700 font-bold px-5 py-3 rounded-md hover:bg-gray-300 transition-colors duration-300 uppercase text-sm">
                        Mon Compte
                    </a>
                {% endif %}
            </div>

            <!-- Réseaux sociaux dans le menu mobile -->
            <div class="flex justify-center space-x-6 pt-4 pb-4 border-t border-gray-200">
                <a href="https://www.facebook.com/joellocation"
                   target="_blank"
                   class="text-gray-600 hover:text-joel-red transition-colors duration-300"
                   aria-label="Facebook">
                    <i class="fab fa-facebook-f fa-lg"></i>
                </a>
                <a href="https://www.instagram.com/joel.location/"
                   target="_blank"
                   class="text-gray-600 hover:text-joel-red transition-colors duration-300"
                   aria-label="Instagram">
                    <i class="fab fa-instagram fa-lg"></i>
                </a>
                <a href="https://wa.me/+330767321447"
                   target="_blank"
                   class="text-gray-600 hover:text-joel-red transition-colors duration-300"
                   aria-label="WhatsApp">
                    <i class="fab fa-whatsapp fa-lg"></i>
                </a>
                <a href="https://youtube.com/channel/UCMZZRNgOmDBIghZwg4tTQJQ"
                   target="_blank"
                   class="text-gray-600 hover:text-joel-red transition-colors duration-300"
                   aria-label="YouTube">
                    <i class="fab fa-youtube fa-lg"></i>
                </a>
            </div>
        </div>
    </div>
</nav>

<script>
    // Mobile menu toggle
    document.addEventListener('DOMContentLoaded', () => {
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        if (mobileMenuButton && mobileMenu) {
            mobileMenuButton.addEventListener('click', (e) => {
                e.stopPropagation();
                mobileMenu.classList.toggle('hidden');
            });
        }

        // Close mobile menu when clicking outside
        document.addEventListener('click', (e) => {
            if (mobileMenu && !mobileMenu.contains(e.target) && mobileMenuButton && !mobileMenuButton.contains(e.target) && !mobileMenu.classList.contains('hidden')) {
                mobileMenu.classList.add('hidden');
            }
        });
    });
</script>