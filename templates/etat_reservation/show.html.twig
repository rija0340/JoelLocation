{% extends 'base2.html.twig' %}

{% block title %}EtatReservation{% endblock %}

{% block content %}
    <h1>Etat de la reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat_reservation.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ etat_reservation.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etat_reservation_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> retour</a>
    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('etat_reservation_edit', {'id': etat_reservation.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
    {% endif %}
    {% if is_granted("ROLE_ADMIN") %}
        {{ include('etat_reservation/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
