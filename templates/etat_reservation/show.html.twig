{% extends 'base2.html.twig' %}

{% block title %}EtatReservation{% endblock %}

{% block content %}
    <h1>Etat de la reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat_reservation.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ etat_reservation.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('etat_reservation_index') }}">retour</a>

    <a href="{{ path('etat_reservation_edit', {'id': etat_reservation.id}) }}">Modifier</a>

    {{ include('etat_reservation/_delete_form.html.twig') }}
{% endblock %}
