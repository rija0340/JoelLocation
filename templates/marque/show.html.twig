{% extends 'base2.html.twig' %}

{% block title %}Marque{% endblock %}

{% block content %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ marque.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('marque_index') }}">retour</a>

    <a href="{{ path('marque_edit', {'id': marque.id}) }}">Modifier</a>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
