{% extends 'base2.html.twig' %}

{% block title %}Marque index{% endblock %}

{% block body %}
    <h1>Liste des marques</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libellé</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for marque in marques %}
            <tr>
                <td>{{ marque.id }}</td>
                <td>{{ marque.libelle }}</td>
                <td>
                    <a href="{{ path('marque_show', {'id': marque.id}) }}">Visionner</a>
                    <a href="{{ path('marque_edit', {'id': marque.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('marque_new') }}">Ajouter</a>
{% endblock %}
