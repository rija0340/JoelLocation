<nav class="navbar navbar-expand-lg navbar-light bg-white mdl-shadow--2dp">
	<a href="{{path('accueil')}}">
		<img src="{{asset('images/Joel-Location-new.png')}}" width="100" height="100" class="d-inline-block align-top" alt="">
	</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse " id="navbarNav">
		<ul class="navbar-nav ml-auto ">
			<li class="nav-item {% if app.request.attributes.get('_route') starts with 'espaceClient_index' %} current {% endif %} ">
				<a class="nav-link" href="{{path('espaceClient_index')}}">
					Mes informations</a>
			</li>
			<li class="nav-item {% if app.request.attributes.get('_route') starts with 'client_reservations' or app.request.attributes.get('_route') starts with 'validation' %} current {% endif %}">
				<a class="nav-link" href="{{path('client_reservations')}}">
					Mes réservations</a>
			</li>

			<li class="nav-item {% if app.request.attributes.get('_route') starts with 'client_step' %} current {% endif %}">
				<a class="nav-link" href="{{path('client_step1')}}">
					Nouvelle réservation</a>
			</li>
			<li class="nav-item {% if app.request.attributes.get('_route') starts with 'client_mesConducteurs' %} current {% endif %}">
				<a class="nav-link" tabindex="-1" class="text-dark" href="{{path('client_mesConducteurs')}}">
					Mes conducteurs</a>
			</li>
			<li class="nav-item dropdown pt-2 ml-3">
				<a class="user-profile dropdown-toggle" aria-haspopup="true" id="navbarDropdown" data-toggle="dropdown" aria-expanded="false">
					{% if app.user %}
						{{ app.user.getNom() }}
					{% endif %}

				</a>
				<div class="dropdown-menu dropdown-usermenu pull-left" aria-labelledby="navbarDropdown">
					<a class="dropdown-item" href="{{path('app_logout')}}">

						<i class="fa fa-sign-out pull-right"></i>
						Deconnexion</a>
				</div>
			</li>

		</ul>
	</div>
</nav>
