{% extends 'base2.html.twig' %}

{% block title %}Type{% endblock %}

{% block content %}
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ type.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> retour</a>

    <a href="{{ path('type_edit', {'id': type.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
