{% extends 'base2.html.twig' %}

{% block title %}ModePaiement{% endblock %}

{% block content %}
    <h1>mode paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_paiement.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ mode_paiement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mode_paiement_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> retour</a>
    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('mode_paiement_edit', {'id': mode_paiement.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
    {% endif %}
    {% if is_granted("ROLE_ADMIN") %}
        {{ include('mode_paiement/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
