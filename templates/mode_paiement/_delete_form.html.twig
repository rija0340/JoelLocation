<form method="post" action="{{ path('mode_paiement_delete', {'id': mode_paiement.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer ce mode de paiement?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_paiement.id) }}">
    <button class="btn">Supprimer</button>
</form>
