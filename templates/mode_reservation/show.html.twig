{% extends 'base2.html.twig' %}

{% block title %}ModeReservation{% endblock %}

{% block content %}
    <h1>Mode de reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mode_reservation.id }}</td>
            </tr>
            <tr>
                <th>Libellé</th>
                <td>{{ mode_reservation.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mode_reservation_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> retour</a>
    {% if is_granted("ROLE_ADMIN") %}
        <a href="{{ path('mode_reservation_edit', {'id': mode_reservation.id}) }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"></i> Modifier</a>
    {% endif %}
    {% if is_granted("ROLE_ADMIN") %}
        {{ include('mode_reservation/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
